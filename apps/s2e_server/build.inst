#!/bin/bash

set -e

MYPWD=`pwd`

. ${MYPWD}/../scripts/include/build.inst.inc

PROG=server
OUTPUT=install/bin/${PROG}

make clean
mkdir -p install/bin
make

mkdir -p /tmp/data

if [ $HAVE_LIBDFT -eq 1 ]; then
    ${LIBDFT}/utils/relink.py ${OUTPUT}
fi

echo "Build completed successfully"